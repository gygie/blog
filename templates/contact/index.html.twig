{% extends 'base.html.twig' %}

{% block title %}Hello ContactController!{% endblock %}

{% block body %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


BIENVENUE DANS L'ECOLE NE FONCTIONNE PAS
    <nav>
        <ul>

            <li><a href="/">Accueil</a></li>
            <li><a href="{{ path('contact')}}">Contact</a></li>

            <ul>
            <li><a href="{{ path('contactCity',{city : 'montpellier'})}}">contacter montpellier</a></li>
            <li><a href="{{ path('contact', {name : "alex"})}}">Contacter de la part d'alex</a></li>
            <li><a href="{{ path('contactCity',{city : 'montpellier', name:"alex"})}}">contacter montpellier de la part d'alex</a></li>
            </ul>
            </li>
        </ul>
    </nav>

    <div class ="contact-container">



        {% if article is defined and article is not null %}

            <ul>
                <li>Id: {{  article.Nom}}</li>
                <li>Nom: {{ article.Slug }}</li>
                <li>Slug: {{ article.Contenu }}</li>
            </ul>

        {% endif %}

            <ul>

                {% if articles is defined %}
                {% for article in articles %}
                <li>
                    <a href="{{ path('article', {'id': article.id}) }}">
                        Mes articles {{ article.Nom }}
                    </a>
                </li>

            </ul>

        {% endfor %}
        {% endif %}
        <a href="{{ path('contact_show') }}">Créer un article</a>
    </div>

{% endblock %}
